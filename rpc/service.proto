syntax = "proto3";
option go_package = "main";

service RegBox {
    rpc Register(AcccountRequest) returns (Response);
    rpc Authenticate(AcccountRequest) returns (Response);
}

message AcccountRequest {
    string login = 1;
    string password = 2;
}

message Response {
    string error = 1;
}
