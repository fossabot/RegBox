syntax = "proto3";
option go_package = "main";

service RegBox { rpc Register(RegisterRequest) returns (RegisterResponse); }

message RegisterRequest {
    string login = 1;
    string password = 2;
}

message RegisterResponse {
    bool successful = 1;
}
